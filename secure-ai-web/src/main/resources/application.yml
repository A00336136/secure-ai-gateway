spring:
  application:
    name: secure-ai-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # JPA defaults (overridden per profile)
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  # Flyway Migrations
  flyway:
    enabled: true
    locations: classpath:db/migration

  # Jackson
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

# ═══════════════════ Server ═══════════════════
server:
  port: 8080
  error:
    include-message: never
    include-binding-errors: never

# ═══════════════════ JWT ═══════════════════
jwt:
  secret: ${JWT_SECRET}   # MUST be set via env var — no fallback (Zero Trust)
  expiration: 3600000     # 1 hour

# ═══════════════════ Ollama ═══════════════════
ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  model: ${OLLAMA_MODEL:llama3.1:8b}
  timeout-seconds: 120
  react:
    max-steps: 10

# ═══════════════════ Rate Limiting ═══════════════════
rate-limit:
  capacity: 100
  refill-tokens: 100
  refill-duration-minutes: 60

# ═══════════════════ 3-Layer Guardrails ═══════════════════
guardrails:
  nemo:
    base-url: ${NEMO_GUARDRAILS_URL:http://localhost:8001}
    timeout-ms: 5000
    enabled: true
  llamaguard:
    model: ${LLAMAGUARD_MODEL:llamaguard3:8b}
    timeout-ms: 5000
    enabled: true
  presidio:
    base-url: ${PRESIDIO_URL:http://localhost:5002}
    timeout-ms: 3000
    enabled: true
    language: en
    min-score: 0.6

# ═══════════════════ PII Redaction ═══════════════════
pii:
  redaction:
    enabled: true
    redact-in-audit-logs: true

# ═══════════════════ Actuator ═══════════════════
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  prometheus:
    metrics:
      export:
        enabled: true

# ═══════════════════ API Docs ═══════════════════
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

# ═══════════════════ Logging ═══════════════════
logging:
  level:
    com.secureai: INFO
    org.springframework.security: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
